/* The sky arc: how an orbit fraction becomes a place over the range. The
   half-sine rise the page has always drawn, with the tilt term (forks ride
   a captured, slanted path; language lean adds its own). */
export function arcPos(f: number, alt: number, tilt: number, W: number, H: number):
  { x: number; y: number; up: number } {
  throw new Error("todo: arcPos");
}
